// env.js — copy this to env.js and fill in your real values
// NEVER commit env.js to git (it's in .gitignore)
// This file exposes your Supabase public keys to the browser.
// The anon key is safe to expose — RLS protects your data.

window.ENV_SUPABASE_URL      = 'YOUR_SUPABASE_PROJECT_URL'
window.ENV_SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY'